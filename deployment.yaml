apiVersion: "apps/v1"
kind: "Deployment"
metadata:
  name: "sample"
  namespace: "default"
  labels:
    app: "express"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "express"
  template:
    metadata:
      labels:
        app: "express"
    spec:
      containers:
      - name: "express-app-sha256-1"
        image: "us-east1-docker.pkg.dev/jenkins-poc-402417/hello-repo/express-app:tagversion"
